# Backend environment variables
NODE_ENV=production
PORT=10000

# Render will inject this when you create a PostgreSQL instance and link it
DATABASE_URL=postgres://USER:PASSWORD@HOST:PORT/DBNAME

# Generate a strong secret for JWTs
JWT_SECRET=please-change-me
RUN_MIGRATIONS=true
